# List: storing survey results
survey_results = ["Yes", "No", "Maybe", "Yes"]
print(survey_results)

# Set: keeping track of unique items
unique_items = {"apple", "banana", "orange"}
print(unique_items)

# Tuple: representing coordinates on a map
point = (3.0, 4.0)
print(point)